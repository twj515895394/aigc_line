interface:
  display_name: "场景参考图生成"
  short_description: "先生成无人环境底图，再参考底图生成正面、左侧、右侧、背面四宫格"
  default_prompt: "Use $environment-reference-generation to generate an empty base environment image first, then use it as the sole image reference for a 2×2 Front, Left, Right, and Back view grid."
