name: CI

on:
  pull_request:
    branches:
      - main
    paths:
      - '*.md'
      - '**/*.md'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  lint-markdown:
    name: Lint Markdown
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive

      - name: Lint markdown
        run: npx --yes markdownlint-cli@0.48.0 '**/*.md' --ignore node_modules --ignore resources/claude-plugin/skills/image-prompt-skill --ignore resources/claude-plugin/skills/video-prompt-skill --disable MD013
